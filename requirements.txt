# Pinned so behavior (especially timestamp handling from entsoe-py) stays
# reproducible across machines and over time. See CLAUDE.md for install/run
# instructions; see the audit findings for why the entsoe-py version matters.
pandas==3.0.5
numpy==2.4.6
matplotlib==3.11.1
statsmodels==0.15.0
linearmodels==7.0
requests==2.33.1
entsoe-py==0.8.0
pytest==9.1.1
